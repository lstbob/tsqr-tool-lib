name: Trigger .NET Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  call-build:
    uses: ./project/build/dotnet.yaml
    with:
      dotnet_version: "9.0.x"

